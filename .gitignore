__pycache__/
*.pyc
.env
logs/

# runtime job queue output
video-queue/
reel-queue/
music-pool/
test-footage/

# local runtime settings (may hold OAuth client secret — never commit)
sortie_settings.json
rtklib/rnx2rtkp.exe

# Session and CLI scratch (regenerated per session — not shared state)
.claude/project-state.json
.claude/session-handoff.md
.planning/agent-history.json
supabase/.temp/
