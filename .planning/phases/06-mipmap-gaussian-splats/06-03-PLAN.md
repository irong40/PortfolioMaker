---
phase: 06-mipmap-gaussian-splats
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false
requirements: [MIP-01, MIP-02, MIP-03, MIP-05]

must_haves:
  truths:
    - "GUI launches with MipMap indicator visible"
    - "Gaussian Splat radio button appears in job type list"
    - "Selecting Gaussian Splat updates description to mention MipMap"
    - "Full test suite passes with no regressions"
  artifacts: []
  key_links: []
---

<objective>
Verify the full MipMap integration works end-to-end: GUI launches cleanly, MipMap indicator shows, Gaussian Splat job type is selectable, and all tests pass.

Purpose: Catch visual issues and confirm the integration before marking the phase complete.
Output: Verified working GUI with MipMap integration.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-mipmap-gaussian-splats/06-01-SUMMARY.md
@.planning/phases/06-mipmap-gaussian-splats/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify imports</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions:
    1. `python -m pytest -x -v` — all tests must pass
    2. `python -c "from mipmap_service import check_mipmap, build_splat_task_json, run_mipmap_pipeline, copy_splat_outputs; print('mipmap_service OK')"` — all exports importable
    3. `python -c "from odm_presets import JOB_TYPES, PRESETS; assert ('gaussian_splat', 'Gaussian Splat') in JOB_TYPES; assert PRESETS['gaussian_splat']['engine'] == 'mipmap'; print('Preset OK')"` — preset correct
    4. `python -c "from report_generator import REPORT_TYPES; assert 'gaussian_splat' in REPORT_TYPES; print('Report OK')"` — report type registered
    5. `python -c "from portfolio_service import submit_to_mipmap; print('Service OK')"` — MipMap routing available

    If any failures, fix them before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd D:/Projects/PortfolioMaker && python -m pytest -x -v</automated>
  </verify>
  <done>All tests pass. All new modules importable. No regressions in existing 67 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MipMap Gaussian Splat integration: 7th job type in GUI, MipMap status indicator, routing to MipMap engine, splat-specific report template</what-built>
  <how-to-verify>
    1. Launch the app: `cd D:\Projects\PortfolioMaker && python portfolio_maker.py`
    2. Verify the header shows TWO status indicators: "NodeODM" (red or green depending on Docker) AND "MipMap" (green if MipMap Desktop installed, red if not)
    3. In Job Configuration, verify 7 radio buttons now appear — the 7th is "Gaussian Splat"
    4. Select "Gaussian Splat" — description should read "3D Gaussian Splat via MipMap Desktop | Uses all photos | via MipMap Desktop"
    5. Select a photo folder, enter a site name, click "Process"
       - If MipMap is NOT installed: should show error dialog "MipMap Not Found"
       - If MipMap IS installed: should begin processing (this is the live smoke test for open questions about image_meta_data and reconstruct_type)
    6. Switch back to "Construction Progress" — confirm it still shows NodeODM-related description and Process still checks NodeODM
    7. Close the app
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
Full test suite green + human visual confirmation of GUI.
</verification>

<success_criteria>
- All tests pass (67 existing + new MipMap tests)
- GUI launches without error
- MipMap indicator visible in header
- Gaussian Splat job type selectable
- Correct error handling when MipMap not installed
</success_criteria>

<output>
After completion, create `.planning/phases/06-mipmap-gaussian-splats/06-03-SUMMARY.md`
</output>
